<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Hiyswa Jambu Homestay · Arau, Perlis</title>
<meta name="description" content="Hiyswa Jambu Homestay in Arau, Perlis — 8 units, 16 bedrooms, swimming pool, mini water park, and more. A serene getaway in northern Malaysia.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,300..900;1,9..144,300..900&family=Manrope:wght@300..800&display=swap" rel="stylesheet">
<script src="https://cdn.tailwindcss.com"></script>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<style>
  :root {
    --cream: #F4ECDB;
    --cream-2: #EDE1C7;
    --paper: #FBF6EA;
    --forest: #1F352C;
    --forest-2: #2D4A3E;
    --ink: #1B2A26;
    --terra: #C75D3C;
    --terra-dark: #A8482A;
    --gold: #C9A35C;
    --blush: #E8B5A6;
    --muted: rgba(27, 42, 38, 0.6);
  }
  * { margin: 0; padding: 0; box-sizing: border-box; }
  html { scroll-behavior: smooth; }
  body {
    font-family: 'Manrope', sans-serif;
    background: var(--cream);
    color: var(--ink);
    overflow-x: hidden;
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
  }
  .font-display { font-family: 'Fraunces', serif; }
  .text-cream { color: var(--cream); }
  .text-forest { color: var(--forest); }
  .text-terra { color: var(--terra); }
  .text-gold { color: var(--gold); }
  .text-muted { color: var(--muted); }
  .bg-cream { background: var(--cream); }
  .bg-cream-2 { background: var(--cream-2); }
  .bg-paper { background: var(--paper); }
  .bg-forest { background: var(--forest); }
  .bg-terra { background: var(--terra); }
  .bg-gold { background: var(--gold); }
  .border-forest { border-color: var(--forest); }
  .border-terra { border-color: var(--terra); }

  /* Texture overlay */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='3' /%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.5'/%3E%3C/svg%3E");
    opacity: 0.04;
    pointer-events: none;
    z-index: 1;
    mix-blend-mode: multiply;
  }

  /* Loader */
  .loader {
    position: fixed; inset: 0; background: var(--forest);
    z-index: 9999; display: flex; align-items: center; justify-content: center;
    transition: opacity 0.8s ease, visibility 0.8s ease;
  }
  .loader.hidden { opacity: 0; visibility: hidden; }
  .loader-mark {
    font-family: 'Fraunces', serif; color: var(--cream);
    font-size: clamp(2rem, 6vw, 4rem); font-style: italic; font-weight: 300;
    overflow: hidden; display: flex; gap: 0.05em;
  }
  .loader-mark span {
    display: inline-block; transform: translateY(110%);
    animation: loaderUp 0.8s cubic-bezier(0.65, 0, 0.35, 1) forwards;
  }
  @keyframes loaderUp { to { transform: translateY(0); } }
  .loader-bar {
    position: absolute; bottom: 0; left: 0; height: 2px; background: var(--terra);
    width: 0; animation: loaderBar 1.6s ease forwards;
  }
  @keyframes loaderBar { to { width: 100%; } }

  /* Custom cursor */
  .cursor-dot, .cursor-ring {
    position: fixed; top: 0; left: 0; pointer-events: none; z-index: 9998;
    border-radius: 50%; transform: translate(-50%, -50%);
    transition: opacity 0.3s ease;
  }
  .cursor-dot { width: 6px; height: 6px; background: var(--terra); }
  .cursor-ring {
    width: 36px; height: 36px; border: 1px solid var(--forest);
    transition: transform 0.2s ease, width 0.3s ease, height 0.3s ease, border-color 0.3s ease, background 0.3s ease;
  }
  .cursor-ring.hover { width: 64px; height: 64px; background: rgba(199,93,60,0.1); border-color: var(--terra); }
  @media (max-width: 1024px) { .cursor-dot, .cursor-ring { display: none; } }

  /* Navigation */
  .nav-wrap {
    position: fixed; top: 0; left: 0; right: 0; z-index: 100;
    transition: padding 0.4s ease, background 0.4s ease, backdrop-filter 0.4s ease;
    padding: 1.5rem 0;
  }
  .nav-wrap.scrolled {
    padding: 0.75rem 0;
    background: rgba(244, 236, 219, 0.85);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(31, 53, 44, 0.08);
  }
  .nav-link {
    position: relative; font-size: 0.85rem; font-weight: 500;
    letter-spacing: 0.08em; text-transform: uppercase; color: var(--forest);
    padding: 0.4rem 0; transition: color 0.3s ease;
  }
  .nav-link::after {
    content: ''; position: absolute; bottom: 0; left: 0; height: 1px;
    width: 0; background: var(--terra); transition: width 0.4s cubic-bezier(0.65, 0, 0.35, 1);
  }
  .nav-link:hover::after, .nav-link.active::after { width: 100%; }
  .nav-link:hover { color: var(--terra); }

  /* Announcement bar */
  .announce {
    background: var(--forest); color: var(--cream);
    font-size: 0.75rem; letter-spacing: 0.12em; text-transform: uppercase;
    overflow: hidden; position: relative; z-index: 90;
  }
  .marquee-track {
    display: inline-flex; gap: 4rem; padding: 0.6rem 0;
    animation: marquee 28s linear infinite; white-space: nowrap;
  }
  @keyframes marquee { to { transform: translateX(-50%); } }
  .marquee-track span { display: inline-flex; align-items: center; gap: 0.75rem; }
  .marquee-track i { color: var(--terra); }

  /* Hero */
  .hero {
    position: relative; min-height: 100vh; overflow: hidden;
    display: flex; align-items: flex-end;
  }
  .hero-bg {
    position: absolute; inset: 0; z-index: 0;
  }
  .hero-bg img {
    width: 100%; height: 100%; object-fit: cover;
    animation: heroZoom 18s ease-in-out infinite alternate;
  }
  @keyframes heroZoom { to { transform: scale(1.12); } }
  .hero-bg::after {
    content: ''; position: absolute; inset: 0;
    background: linear-gradient(180deg, rgba(31,53,44,0.4) 0%, rgba(31,53,44,0.2) 40%, rgba(31,53,44,0.85) 100%);
  }
  .hero-content {
    position: relative; z-index: 2; padding: 0 2rem 6rem;
    max-width: 1400px; margin: 0 auto; width: 100%;
  }
  .hero-eyebrow {
    display: inline-flex; align-items: center; gap: 0.75rem;
    color: var(--cream); font-size: 0.75rem; letter-spacing: 0.3em;
    text-transform: uppercase; margin-bottom: 2rem;
    opacity: 0; animation: fadeUp 1s 0.3s ease forwards;
  }
  .hero-eyebrow::before, .hero-eyebrow::after {
    content: ''; width: 2.5rem; height: 1px; background: var(--cream); opacity: 0.5;
  }
  .hero-title {
    font-family: 'Fraunces', serif; color: var(--cream);
    font-size: clamp(3.5rem, 11vw, 11rem); line-height: 0.92; font-weight: 300;
    letter-spacing: -0.02em;
  }
  .hero-title .word { display: inline-block; overflow: hidden; vertical-align: top; }
  .hero-title .word span {
    display: inline-block; transform: translateY(110%);
    animation: heroWord 1s cubic-bezier(0.65, 0, 0.35, 1) forwards;
  }
  .hero-title .word:nth-child(1) span { animation-delay: 0.5s; }
  .hero-title .word:nth-child(2) span { animation-delay: 0.7s; }
  .hero-title .word:nth-child(2) { font-style: italic; font-weight: 400; color: var(--blush); }
  @keyframes heroWord { to { transform: translateY(0); } }
  .hero-sub {
    color: var(--cream); opacity: 0.9; max-width: 540px;
    font-size: 1.05rem; margin-top: 2rem;
    opacity: 0; animation: fadeUp 1s 1s ease forwards;
  }
  .hero-cta {
    display: flex; gap: 1rem; margin-top: 2.5rem; flex-wrap: wrap;
    opacity: 0; animation: fadeUp 1s 1.2s ease forwards;
  }
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  .btn {
    display: inline-flex; align-items: center; gap: 0.75rem;
    padding: 1rem 2rem; font-size: 0.85rem; letter-spacing: 0.12em;
    text-transform: uppercase; font-weight: 500; border-radius: 999px;
    transition: all 0.4s cubic-bezier(0.65, 0, 0.35, 1); cursor: none;
    border: 1px solid transparent;
  }
  .btn-primary { background: var(--terra); color: var(--cream); }
  .btn-primary:hover { background: var(--forest); transform: translateY(-2px); box-shadow: 0 10px 30px rgba(31,53,44,0.2); }
  .btn-ghost { background: transparent; color: var(--cream); border-color: rgba(244,236,219,0.4); }
  .btn-ghost:hover { background: var(--cream); color: var(--forest); border-color: var(--cream); }
  .btn-dark { background: var(--forest); color: var(--cream); }
  .btn-dark:hover { background: var(--terra); transform: translateY(-2px); }

  /* Rotating badge */
  .rotating-badge {
    position: absolute; right: 3rem; bottom: 6rem; z-index: 3;
    width: 140px; height: 140px;
    opacity: 0; animation: fadeIn 1s 1.5s ease forwards;
  }
  @keyframes fadeIn { to { opacity: 1; } }
  .rotating-badge svg { animation: rotate 18s linear infinite; }
  @keyframes rotate { to { transform: rotate(360deg); } }
  .rotating-badge .badge-inner {
    position: absolute; inset: 0; display: flex; align-items: center; justify-content: center;
  }
  .rotating-badge .badge-inner i {
    color: var(--cream); font-size: 1.5rem;
    animation: bob 2s ease-in-out infinite;
  }
  @keyframes bob { 50% { transform: translateY(-6px); } }

  /* Scroll indicator */
  .scroll-indicator {
    position: absolute; left: 2rem; bottom: 2rem; z-index: 3;
    color: var(--cream); font-size: 0.7rem; letter-spacing: 0.3em;
    text-transform: uppercase; writing-mode: vertical-rl;
    display: flex; align-items: center; gap: 1rem;
    opacity: 0; animation: fadeIn 1s 1.8s ease forwards;
  }
  .scroll-indicator::after {
    content: ''; width: 1px; height: 60px; background: var(--cream);
    animation: scrollLine 2s ease-in-out infinite;
    transform-origin: top;
  }
  @keyframes scrollLine { 50% { transform: scaleY(0.3); } }

  /* Section headers */
  .section { padding: 8rem 2rem; position: relative; z-index: 2; }
  .section-inner { max-width: 1400px; margin: 0 auto; }
  .section-num {
    font-family: 'Fraunces', serif; font-size: 0.85rem; letter-spacing: 0.3em;
    color: var(--terra); text-transform: uppercase; margin-bottom: 1.5rem;
    display: flex; align-items: center; gap: 1rem;
  }
  .section-num::before { content: ''; width: 2rem; height: 1px; background: var(--terra); }
  .section-title {
    font-family: 'Fraunces', serif; font-size: clamp(2.5rem, 6vw, 5rem);
    line-height: 1; font-weight: 300; letter-spacing: -0.02em; color: var(--forest);
  }
  .section-title em { font-style: italic; color: var(--terra); font-weight: 400; }

  /* Reveal animation */
  .reveal { opacity: 0; transform: translateY(40px); transition: opacity 1s cubic-bezier(0.65, 0, 0.35, 1), transform 1s cubic-bezier(0.65, 0, 0.35, 1); }
  .reveal.in { opacity: 1; transform: translateY(0); }
  .reveal-delay-1 { transition-delay: 0.15s; }
  .reveal-delay-2 { transition-delay: 0.3s; }
  .reveal-delay-3 { transition-delay: 0.45s; }

  /* Image clip reveal */
  .img-reveal { position: relative; overflow: hidden; }
  .img-reveal img {
    width: 100%; height: 100%; object-fit: cover;
    transition: transform 1.2s cubic-bezier(0.65, 0, 0.35, 1);
  }
  .img-reveal::before {
    content: ''; position: absolute; inset: 0; background: var(--forest);
    transform: translateX(0); transition: transform 1s cubic-bezier(0.85, 0, 0.15, 1);
    z-index: 2;
  }
  .img-reveal.in::before { transform: translateX(101%); }
  .img-reveal.in img { transform: scale(1); }
  .img-reveal img { transform: scale(1.15); }

  /* About */
  .about-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 4rem; align-items: center; }
  @media (max-width: 900px) { .about-grid { grid-template-columns: 1fr; gap: 3rem; } }
  .about-imgs { position: relative; height: 620px; }
  .about-imgs .img-1 { position: absolute; top: 0; left: 0; width: 70%; height: 70%; }
  .about-imgs .img-2 { position: absolute; bottom: 0; right: 0; width: 60%; height: 55%; border: 8px solid var(--cream); }
  .about-imgs .badge {
    position: absolute; top: 50%; right: -1rem; transform: translateY(-50%);
    background: var(--terra); color: var(--cream); padding: 1.25rem 1.5rem;
    font-family: 'Fraunces', serif; font-style: italic; font-size: 1.5rem;
    border-radius: 50%; width: 110px; height: 110px;
    display: flex; align-items: center; justify-content: center; text-align: center;
    line-height: 1; z-index: 3;
  }
  .stats-grid {
    display: grid; grid-template-columns: repeat(4, 1fr); gap: 1px;
    background: rgba(31,53,44,0.15); margin-top: 4rem; border-top: 1px solid rgba(31,53,44,0.15); border-bottom: 1px solid rgba(31,53,44,0.15);
  }
  .stat { background: var(--cream); padding: 2rem 1.5rem; }
  .stat-num { font-family: 'Fraunces', serif; font-size: 2.75rem; font-weight: 300; color: var(--forest); line-height: 1; }
  .stat-num em { font-style: italic; color: var(--terra); }
  .stat-label { font-size: 0.75rem; letter-spacing: 0.15em; text-transform: uppercase; color: var(--muted); margin-top: 0.5rem; }
  @media (max-width: 700px) { .stats-grid { grid-template-columns: repeat(2, 1fr); } }

  /* Rooms */
  .rooms-section { background: var(--forest); color: var(--cream); }
  .rooms-section .section-title { color: var(--cream); }
  .rooms-section .section-num { color: var(--gold); }
  .rooms-section .section-num::before { background: var(--gold); }
  .room-intro {
    display: grid; grid-template-columns: 1fr 1fr; gap: 4rem; margin: 4rem 0 5rem;
  }
  @media (max-width: 900px) { .room-intro { grid-template-columns: 1fr; gap: 2rem; } }
  .room-features {
    display: grid; grid-template-columns: repeat(2, 1fr); gap: 1rem;
  }
  .room-feature {
    display: flex; gap: 1rem; align-items: flex-start;
    padding: 1rem; border: 1px solid rgba(244,236,219,0.15); border-radius: 12px;
    transition: all 0.3s ease;
  }
  .room-feature:hover { border-color: var(--gold); background: rgba(201,163,92,0.05); }
  .room-feature i { color: var(--gold); font-size: 1.25rem; margin-top: 0.2rem; }
  .room-feature-title { font-weight: 600; font-size: 0.95rem; }
  .room-feature-sub { font-size: 0.8rem; opacity: 0.65; }

  .room-tiles { display: grid; grid-template-columns: 1fr 1fr; gap: 2rem; }
  @media (max-width: 900px) { .room-tiles { grid-template-columns: 1fr; } }
  .room-tile {
    background: var(--cream); color: var(--ink);
    border-radius: 24px; overflow: hidden; position: relative;
    transition: transform 0.5s cubic-bezier(0.65, 0, 0.35, 1), box-shadow 0.5s ease;
  }
  .room-tile:hover { transform: translateY(-8px); box-shadow: 0 30px 60px rgba(0,0,0,0.3); }
  .room-tile-img { height: 260px; overflow: hidden; position: relative; }
  .room-tile-img img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.8s ease; }
  .room-tile:hover .room-tile-img img { transform: scale(1.08); }
  .room-tile-tag {
    position: absolute; top: 1.25rem; left: 1.25rem;
    background: var(--terra); color: var(--cream);
    padding: 0.4rem 0.9rem; border-radius: 999px;
    font-size: 0.7rem; letter-spacing: 0.15em; text-transform: uppercase; font-weight: 600;
  }
  .room-tile-tag.gold { background: var(--gold); color: var(--forest); }
  .room-tile-body { padding: 2rem; }
  .room-tile-title { font-family: 'Fraunces', serif; font-size: 1.75rem; font-weight: 400; color: var(--forest); }
  .room-tile-days { font-size: 0.8rem; letter-spacing: 0.1em; text-transform: uppercase; color: var(--muted); margin-top: 0.25rem; }
  .room-tile-price {
    display: flex; align-items: baseline; gap: 0.5rem; margin: 1.5rem 0;
    padding: 1.25rem 0; border-top: 1px solid rgba(31,53,44,0.1); border-bottom: 1px solid rgba(31,53,44,0.1);
  }
  .room-tile-price .currency { font-size: 1rem; color: var(--terra); font-weight: 600; }
  .room-tile-price .amount { font-family: 'Fraunces', serif; font-size: 3rem; font-weight: 300; color: var(--forest); line-height: 1; }
  .room-tile-price .unit { font-size: 0.8rem; color: var(--muted); }
  .room-tile-breakdown { display: flex; gap: 1.5rem; margin-bottom: 1.5rem; font-size: 0.85rem; }
  .room-tile-breakdown div { display: flex; flex-direction: column; }
  .room-tile-breakdown .label { font-size: 0.7rem; letter-spacing: 0.1em; text-transform: uppercase; color: var(--muted); }
  .room-tile-breakdown .val { font-weight: 600; color: var(--forest); }
  .room-tile-cta {
    display: flex; align-items: center; justify-content: space-between;
    background: var(--forest); color: var(--cream); padding: 1rem 1.5rem;
    border-radius: 999px; transition: all 0.3s ease; cursor: none;
  }
  .room-tile-cta:hover { background: var(--terra); }
  .room-tile-cta span { font-size: 0.85rem; letter-spacing: 0.1em; text-transform: uppercase; font-weight: 500; }
  .room-tile-cta i { transition: transform 0.3s ease; }
  .room-tile-cta:hover i { transform: translateX(4px); }

  /* Facilities */
  .facilities-section { background: var(--cream); }
  .facilities-grid {
    display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem; margin-top: 4rem;
  }
  @media (max-width: 900px) { .facilities-grid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 600px) { .facilities-grid { grid-template-columns: 1fr; } }
  .facility-card {
    position: relative; border-radius: 20px; overflow: hidden;
    aspect-ratio: 4/5; cursor: none;
  }
  .facility-card.tall { aspect-ratio: 4/6; }
  .facility-card img {
    width: 100%; height: 100%; object-fit: cover;
    transition: transform 1s cubic-bezier(0.65, 0, 0.35, 1);
  }
  .facility-card:hover img { transform: scale(1.1); }
  .facility-card::after {
    content: ''; position: absolute; inset: 0;
    background: linear-gradient(180deg, transparent 30%, rgba(31,53,44,0.9) 100%);
  }
  .facility-content {
    position: absolute; bottom: 0; left: 0; right: 0; padding: 2rem;
    color: var(--cream); z-index: 2;
  }
  .facility-icon {
    display: inline-flex; align-items: center; justify-content: center;
    width: 48px; height: 48px; border-radius: 50%;
    background: rgba(244,236,219,0.15); backdrop-filter: blur(10px);
    border: 1px solid rgba(244,236,219,0.3); margin-bottom: 1rem;
    transition: all 0.4s ease;
  }
  .facility-card:hover .facility-icon { background: var(--terra); border-color: var(--terra); transform: translateY(-4px); }
  .facility-icon i { color: var(--cream); font-size: 1.1rem; }
  .facility-name { font-family: 'Fraunces', serif; font-size: 1.5rem; font-weight: 400; }
  .facility-desc { font-size: 0.85rem; opacity: 0.85; margin-top: 0.5rem; max-height: 0; overflow: hidden; transition: max-height 0.5s ease, margin-top 0.5s ease; }
  .facility-card:hover .facility-desc { max-height: 100px; }
  .facility-num {
    position: absolute; top: 1.5rem; right: 1.5rem; z-index: 2;
    font-family: 'Fraunces', serif; font-style: italic; font-size: 0.85rem;
    color: var(--cream); opacity: 0.7;
  }

  /* Gallery */
  .gallery-section { background: var(--paper); position: relative; overflow: hidden; }
  .gallery-intro { display: grid; grid-template-columns: 1fr 1fr; gap: 4rem; align-items: end; margin-bottom: 4rem; }
  @media (max-width: 900px) { .gallery-intro { grid-template-columns: 1fr; gap: 2rem; } }
  .gallery-grid {
    display: grid; grid-template-columns: repeat(4, 1fr); gap: 1rem;
    grid-auto-rows: 200px;
  }
  @media (max-width: 900px) { .gallery-grid { grid-template-columns: repeat(2, 1fr); } }
  .gallery-item {
    position: relative; overflow: hidden; border-radius: 12px; cursor: none;
  }
  .gallery-item img { width: 100%; height: 100%; object-fit: cover; transition: transform 1s cubic-bezier(0.65, 0, 0.35, 1); }
  .gallery-item:hover img { transform: scale(1.08); }
  .gallery-item::after {
    content: ''; position: absolute; inset: 0;
    background: linear-gradient(180deg, transparent 50%, rgba(31,53,44,0.85) 100%);
    opacity: 0; transition: opacity 0.4s ease;
  }
  .gallery-item:hover::after { opacity: 1; }
  .gallery-caption {
    position: absolute; bottom: 1.25rem; left: 1.25rem; right: 1.25rem;
    color: var(--cream); z-index: 2;
    opacity: 0; transform: translateY(10px); transition: all 0.4s ease;
  }
  .gallery-item:hover .gallery-caption { opacity: 1; transform: translateY(0); }
  .gallery-caption .cat { font-size: 0.7rem; letter-spacing: 0.15em; text-transform: uppercase; color: var(--gold); }
  .gallery-caption .title { font-family: 'Fraunces', serif; font-size: 1.1rem; font-style: italic; margin-top: 0.25rem; }
  .gallery-item.h-wide { grid-column: span 2; }
  .gallery-item.h-tall { grid-row: span 2; }
  .gallery-item.h-big { grid-column: span 2; grid-row: span 2; }

  /* Night views */
  .night-section {
    background: var(--forest); color: var(--cream);
    padding: 6rem 0; margin: 6rem 0; position: relative; overflow: hidden;
  }
  .night-section::before {
    content: ''; position: absolute; inset: 0;
    background-image: radial-gradient(circle at 20% 30%, rgba(201,163,92,0.15), transparent 40%), radial-gradient(circle at 80% 70%, rgba(199,93,60,0.1), transparent 40%);
    pointer-events: none;
  }
  .night-header { max-width: 1400px; margin: 0 auto 3rem; padding: 0 2rem; }
  .night-track-wrap { overflow: hidden; mask-image: linear-gradient(90deg, transparent, black 5%, black 95%, transparent); }
  .night-track {
    display: flex; gap: 1.5rem; padding: 0 2rem;
    animation: nightScroll 50s linear infinite;
    width: max-content;
  }
  .night-track:hover { animation-play-state: paused; }
  @keyframes nightScroll { to { transform: translateX(-50%); } }
  .night-card {
    flex-shrink: 0; width: 360px; height: 460px;
    position: relative; border-radius: 16px; overflow: hidden;
  }
  .night-card img { width: 100%; height: 100%; object-fit: cover; transition: transform 1s ease; }
  .night-card:hover img { transform: scale(1.08); }
  .night-card::after {
    content: ''; position: absolute; inset: 0;
    background: linear-gradient(180deg, rgba(31,53,44,0.1) 0%, rgba(31,53,44,0.7) 100%);
  }
  .night-card-content {
    position: absolute; bottom: 0; left: 0; right: 0; padding: 1.75rem; z-index: 2; color: var(--cream);
  }
  .night-card-content .time { font-size: 0.7rem; letter-spacing: 0.2em; text-transform: uppercase; color: var(--gold); }
  .night-card-content .title { font-family: 'Fraunces', serif; font-size: 1.5rem; font-style: italic; margin-top: 0.5rem; }

  /* Guest memories polaroid */
  .memories-section { padding: 6rem 2rem; max-width: 1400px; margin: 0 auto; }
  .memories-grid {
    display: grid; grid-template-columns: repeat(3, 1fr); gap: 3rem 2rem; margin-top: 3rem;
  }
  @media (max-width: 900px) { .memories-grid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 600px) { .memories-grid { grid-template-columns: 1fr; } }
  .polaroid {
    background: var(--paper); padding: 1rem 1rem 2rem;
    box-shadow: 0 15px 35px rgba(31,53,44,0.15);
    transition: transform 0.4s ease;
    position: relative;
  }
  .polaroid:nth-child(odd) { transform: rotate(-2deg); }
  .polaroid:nth-child(even) { transform: rotate(1.5deg); }
  .polaroid:hover { transform: rotate(0) translateY(-8px); z-index: 2; }
  .polaroid-img { aspect-ratio: 1; overflow: hidden; background: var(--cream-2); }
  .polaroid-img img { width: 100%; height: 100%; object-fit: cover; }
  .polaroid-caption {
    font-family: 'Fraunces', serif; font-style: italic; font-size: 1rem;
    text-align: center; margin-top: 1rem; color: var(--forest);
  }
  .polaroid-meta { font-size: 0.75rem; text-align: center; color: var(--muted); margin-top: 0.25rem; }

  /* Location */
  .location-section { background: var(--cream-2); }
  .location-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 4rem; margin-top: 4rem; align-items: stretch; }
  @media (max-width: 900px) { .location-grid { grid-template-columns: 1fr; } }
  .map-wrap {
    border-radius: 20px; overflow: hidden; min-height: 480px;
    box-shadow: 0 20px 50px rgba(31,53,44,0.15);
    border: 8px solid var(--paper);
  }
  .map-wrap iframe { width: 100%; height: 100%; min-height: 480px; border: 0; display: block; }
  .location-info { display: flex; flex-direction: column; gap: 1.5rem; }
  .location-card {
    background: var(--paper); padding: 2rem; border-radius: 16px;
    display: flex; gap: 1.5rem; align-items: flex-start;
    transition: all 0.3s ease;
  }
  .location-card:hover { transform: translateX(8px); box-shadow: 0 15px 30px rgba(31,53,44,0.08); }
  .location-card .icon {
    flex-shrink: 0; width: 52px; height: 52px; border-radius: 50%;
    background: var(--forest); color: var(--cream);
    display: flex; align-items: center; justify-content: center; font-size: 1.1rem;
  }
  .location-card h4 { font-family: 'Fraunces', serif; font-size: 1.25rem; color: var(--forest); margin-bottom: 0.25rem; }
  .location-card p { font-size: 0.95rem; color: var(--muted); }
  .nearby-list { margin-top: 1rem; }
  .nearby-item {
    display: flex; justify-content: space-between; align-items: center;
    padding: 0.85rem 0; border-bottom: 1px dashed rgba(31,53,44,0.15);
  }
  .nearby-item:last-child { border-bottom: 0; }
  .nearly-item-name { font-size: 0.95rem; }
  .nearby-item-dist { font-size: 0.8rem; color: var(--terra); font-weight: 600; }

  /* Contact */
  .contact-section { background: var(--forest); color: var(--cream); position: relative; overflow: hidden; }
  .contact-section::before {
    content: ''; position: absolute; top: -20%; right: -10%;
    width: 600px; height: 600px; border-radius: 50%;
    background: radial-gradient(circle, rgba(199,93,60,0.15), transparent 70%);
  }
  .contact-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 5rem; align-items: center; position: relative; z-index: 2; }
  @media (max-width: 900px) { .contact-grid { grid-template-columns: 1fr; gap: 3rem; } }
  .contact-section .section-title { color: var(--cream); }
  .contact-section .section-num { color: var(--gold); }
  .contact-section .section-num::before { background: var(--gold); }
  .contact-form { display: flex; flex-direction: column; gap: 1.25rem; }
  .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1.25rem; }
  @media (max-width: 600px) { .form-row { grid-template-columns: 1fr; } }
  .form-field { display: flex; flex-direction: column; }
  .form-field label { font-size: 0.7rem; letter-spacing: 0.15em; text-transform: uppercase; color: var(--gold); margin-bottom: 0.5rem; }
  .form-field input, .form-field select, .form-field textarea {
    background: transparent; border: 0; border-bottom: 1px solid rgba(244,236,219,0.3);
    color: var(--cream); padding: 0.75rem 0; font-family: 'Manrope', sans-serif; font-size: 1rem;
    transition: border-color 0.3s ease;
  }
  .form-field input:focus, .form-field select:focus, .form-field textarea:focus {
    outline: 0; border-color: var(--gold);
  }
  .form-field select option { background: var(--forest); }
  .contact-cta { display: flex; gap: 1rem; margin-top: 3rem; flex-wrap: wrap; }
  .contact-channel {
    display: flex; align-items: center; gap: 1rem;
    padding: 1rem 1.5rem; border: 1px solid rgba(244,236,219,0.2); border-radius: 999px;
    color: var(--cream); transition: all 0.3s ease;
  }
  .contact-channel:hover { background: var(--cream); color: var(--forest); border-color: var(--cream); }
  .contact-channel i { font-size: 1.2rem; }
  .contact-channel .label { font-size: 0.7rem; letter-spacing: 0.1em; text-transform: uppercase; opacity: 0.7; }
  .contact-channel .val { font-weight: 600; }

  /* Footer */
  footer { background: var(--ink); color: var(--cream); padding: 5rem 2rem 2rem; }
  .footer-inner { max-width: 1400px; margin: 0 auto; }
  .footer-grid { display: grid; grid-template-columns: 2fr 1fr 1fr 1fr; gap: 3rem; }
  @media (max-width: 900px) { .footer-grid { grid-template-columns: 1fr 1fr; } }
  @media (max-width: 600px) { .footer-grid { grid-template-columns: 1fr; } }
  .footer-brand { font-family: 'Fraunces', serif; font-style: italic; font-size: 2rem; font-weight: 300; }
  .footer-tag { opacity: 0.7; font-size: 0.9rem; margin-top: 1rem; max-width: 320px; }
  .footer-col h5 { font-size: 0.75rem; letter-spacing: 0.2em; text-transform: uppercase; color: var(--gold); margin-bottom: 1.25rem; }
  .footer-col a { display: block; color: var(--cream); opacity: 0.75; font-size: 0.9rem; padding: 0.3rem 0; transition: all 0.3s ease; }
  .footer-col a:hover { opacity: 1; color: var(--terra); transform: translateX(4px); }
  .footer-bottom {
    margin-top: 4rem; padding-top: 2rem; border-top: 1px solid rgba(244,236,219,0.15);
    display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem;
    font-size: 0.8rem; opacity: 0.7;
  }
  .footer-social { display: flex; gap: 0.75rem; }
  .footer-social a {
    width: 38px; height: 38px; border-radius: 50%;
    border: 1px solid rgba(244,236,219,0.2); display: flex; align-items: center; justify-content: center;
    transition: all 0.3s ease;
  }
  .footer-social a:hover { background: var(--terra); border-color: var(--terra); transform: translateY(-3px); }

  /* Back to top */
  .back-top {
    position: fixed; bottom: 2rem; right: 2rem; z-index: 50;
    width: 52px; height: 52px; border-radius: 50%;
    background: var(--terra); color: var(--cream);
    display: flex; align-items: center; justify-content: center;
    opacity: 0; visibility: hidden; transform: translateY(20px);
    transition: all 0.4s ease; cursor: none;
    box-shadow: 0 10px 30px rgba(199,93,60,0.4);
  }
  .back-top.show { opacity: 1; visibility: visible; transform: translateY(0); }
  .back-top:hover { background: var(--forest); transform: translateY(-4px); }

  /* Mobile menu */
  .mobile-toggle {
    display: none; background: transparent; border: 0; cursor: none;
    width: 40px; height: 40px; position: relative;
  }
  .mobile-toggle span {
    position: absolute; left: 8px; right: 8px; height: 2px; background: var(--forest);
    transition: all 0.3s ease;
  }
  .mobile-toggle span:nth-child(1) { top: 14px; }
  .mobile-toggle span:nth-child(2) { top: 20px; }
  .mobile-toggle span:nth-child(3) { top: 26px; }
  .mobile-toggle.open span:nth-child(1) { transform: rotate(45deg); top: 20px; }
  .mobile-toggle.open span:nth-child(2) { opacity: 0; }
  .mobile-toggle.open span:nth-child(3) { transform: rotate(-45deg); top: 20px; }
  @media (max-width: 1024px) {
    .mobile-toggle { display: block; }
    .nav-links {
      position: fixed; top: 0; right: 0; bottom: 0; width: 80%; max-width: 360px;
      background: var(--forest); flex-direction: column; padding: 6rem 2rem 2rem; gap: 1.5rem;
      transform: translateX(100%); transition: transform 0.5s cubic-bezier(0.65, 0, 0.35, 1);
      z-index: 95;
    }
    .nav-links.open { transform: translateX(0); }
    .nav-links .nav-link { color: var(--cream); font-size: 1rem; }
    .nav-cta { display: none; }
  }

  .container-app { position: relative; z-index: 2; }
  a, button { cursor: none; }
  @media (max-width: 1024px) { a, button { cursor: pointer; } }

  /* Selection */
  ::selection { background: var(--terra); color: var(--cream); }

  /* Scrollbar */
  ::-webkit-scrollbar { width: 8px; }
  ::-webkit-scrollbar-track { background: var(--cream); }
  ::-webkit-scrollbar-thumb { background: var(--forest); border-radius: 4px; }
  ::-webkit-scrollbar-thumb:hover { background: var(--terra); }
</style>
</head>
<body>

<!-- Loader -->
<div class="loader" id="loader">
  <div class="loader-mark">
    <span style="animation-delay:0s">H</span>
    <span style="animation-delay:0.05s">i</span>
    <span style="animation-delay:0.1s">y</span>
    <span style="animation-delay:0.15s">s</span>
    <span style="animation-delay:0.2s">w</span>
    <span style="animation-delay:0.25s">a</span>
    <span style="animation-delay:0.3s">&nbsp;</span>
    <span style="animation-delay:0.35s">J</span>
    <span style="animation-delay:0.4s">a</span>
    <span style="animation-delay:0.45s">m</span>
    <span style="animation-delay:0.5s">b</span>
    <span style="animation-delay:0.55s">u</span>
  </div>
  <div class="loader-bar"></div>
</div>

<!-- Custom cursor -->
<div class="cursor-dot" id="cursorDot"></div>
<div class="cursor-ring" id="cursorRing"></div>

<div class="container-app">

<!-- Announcement Bar -->
<div class="announce">
  <div class="marquee-track">
    <span><i class="fas fa-star"></i> Now open for booking in Arau, Perlis</span>
    <span><i class="fas fa-water"></i> Swimming Pool &amp; Mini Water Park</span>
    <span><i class="fas fa-fire"></i> BBQ pit available for guests</span>
    <span><i class="fas fa-bed"></i> 8 units · 16 bedrooms · sleeps up to 32</span>
    <span><i class="fas fa-map-pin"></i> 5 minutes from Arau town</span>
    <span><i class="fas fa-star"></i> Now open for booking in Arau, Perlis</span>
    <span><i class="fas fa-water"></i> Swimming Pool &amp; Mini Water Park</span>
    <span><i class="fas fa-fire"></i> BBQ pit available for guests</span>
    <span><i class="fas fa-bed"></i> 8 units · 16 bedrooms · sleeps up to 32</span>
    <span><i class="fas fa-map-pin"></i> 5 minutes from Arau town</span>
  </div>
</div>

<!-- Navigation -->
<nav class="nav-wrap" id="nav">
  <div class="max-w-[1400px] mx-auto px-8 flex items-center justify-between">
    <a href="#home" class="font-display text-2xl italic font-light text-forest" style="letter-spacing:-0.02em">
      Hiyswa <span class="text-terra">Jambu</span>
    </a>
    <div class="nav-links flex items-center gap-8" id="navLinks">
      <a href="#home" class="nav-link active">Home</a>
      <a href="#about" class="nav-link">About</a>
      <a href="#rooms" class="nav-link">Rooms &amp; Rates</a>
      <a href="#facilities" class="nav-link">Facilities</a>
      <a href="#gallery" class="nav-link">Gallery</a>
      <a href="#location" class="nav-link">Location</a>
      <a href="#contact" class="nav-link">Contact</a>
    </div>
    <a href="#contact" class="btn btn-dark nav-cta" style="padding:0.75rem 1.5rem">
      Book Now <i class="fas fa-arrow-right text-xs"></i>
    </a>
    <button class="mobile-toggle" id="mobileToggle" aria-label="Menu">
      <span></span><span></span><span></span>
    </button>
  </div>
</nav>

<!-- Hero -->
<section id="home" class="hero">
  <div class="hero-bg">
    <img src="https://picsum.photos/seed/homestay-villa-tropical-2024/1920/1200.jpg" alt="Hiyswa Jambu Homestay exterior">
  </div>
  <div class="hero-content">
    <div class="hero-eyebrow">Arau · Perlis · Malaysia</div>
    <h1 class="hero-title">
      <span class="word"><span>Hiyswa</span></span>
      <span class="word"><span>Jambu</span></span>
    </h1>
    <p class="hero-sub">A serene homestay nestled in the quiet town of Arau, where the scent of jambu trees, the warmth of northern Malaysian hospitality, and the comfort of a fully-equipped retreat come together for the slow getaway you've been craving.</p>
    <div class="hero-cta">
      <a href="#rooms" class="btn btn-primary">View Rooms &amp; Rates <i class="fas fa-arrow-right text-xs"></i></a>
      <a href="#gallery" class="btn btn-ghost">Explore Gallery</a>
    </div>
  </div>

  <!-- Rotating badge -->
  <div class="rotating-badge hidden lg:block">
    <svg viewBox="0 0 100 100" width="140" height="140">
      <defs>
        <path id="circlePath" d="M 50, 50 m -40, 0 a 40,40 0 1,1 80,0 a 40,40 0 1,1 -80,0"/>
      </defs>
      <text fill="#F4ECDB" font-size="9" font-family="Manrope" letter-spacing="3">
        <textPath href="#circlePath">HIYSWA JAMBU · ARAU PERLIS · EST. 2023 · </textPath>
      </text>
    </svg>
    <div class="badge-inner"><i class="fas fa-leaf"></i></div>
  </div>

  <div class="scroll-indicator">Scroll to discover</div>
</section>

<!-- About -->
<section id="about" class="section">
  <div class="section-inner">
    <div class="reveal section-num">01 — About Us</div>
    <h2 class="section-title reveal reveal-delay-1">A quiet corner <em>of Perlis,</em><br>made for slowing down.</h2>

    <div class="about-grid mt-16">
      <div class="about-imgs">
        <div class="img-1 img-reveal">
          <img src="https://picsum.photos/seed/jambu-orchard-green/800/700.jpg" alt="Homestay garden">
        </div>
        <div class="img-2 img-reveal">
          <img src="https://picsum.photos/seed/perlis-rural-house/700/600.jpg" alt="Perlis countryside">
        </div>
        <div class="badge">Since<br>2023</div>
      </div>
      <div class="reveal reveal-delay-2">
        <p class="text-lg leading-relaxed text-forest mb-6" style="font-size:1.1rem">
          Hiyswa Jambu Homestay was born from a simple wish — to share the calm of Arau with travellers looking for more than just a place to sleep. Tucked along a quiet road lined with jambu trees, our eight-unit retreat was designed for families, friends, and small groups who want to gather, celebrate, or simply breathe.
        </p>
        <p class="text-muted mb-8">
          Each unit sleeps up to four adults and four children comfortably, with a private kitchen, living room, and the kind of quiet that's getting harder to find. Step outside and you'll find a swimming pool, a mini water park for the little ones, a futsal court for the competitive, BBQ pits for the long evenings, and bicycles for the aimless rides that holidays are made of.
        </p>
        <div class="flex gap-8 mt-8">
          <div>
            <div class="font-display text-3xl italic text-terra">8</div>
            <div class="text-xs uppercase tracking-widest text-muted mt-1">Private units</div>
          </div>
          <div>
            <div class="font-display text-3xl italic text-terra">16</div>
            <div class="text-xs uppercase tracking-widest text-muted mt-1">Bedrooms</div>
          </div>
          <div>
            <div class="font-display text-3xl italic text-terra">6</div>
            <div class="text-xs uppercase tracking-widest text-muted mt-1">On-site facilities</div>
          </div>
        </div>
      </div>
    </div>

    <div class="stats-grid reveal">
      <div class="stat">
        <div class="stat-num"><em>8</em></div>
        <div class="stat-label">Units available</div>
      </div>
      <div class="stat">
        <div class="stat-num"><em>32</em></div>
        <div class="stat-label">Maximum guests</div>
      </div>
      <div class="stat">
        <div class="stat-num"><em>4+4</em></div>
        <div class="stat-label">Adults &amp; children per unit</div>
      </div>
      <div class="stat">
        <div class="stat-num"><em>5</em>min</div>
        <div class="stat-label">From Arau town</div>
      </div>
    </div>
  </div>
</section>

<!-- Rooms & Rates -->
<section id="rooms" class="section rooms-section">
  <div class="section-inner">
    <div class="reveal section-num">02 — Rooms &amp; Rates</div>
    <h2 class="section-title reveal reveal-delay-1">Eight units. <em>Sixteen rooms.</em><br>One restful stay.</h2>

    <div class="room-intro reveal reveal-delay-2">
      <div>
        <p class="text-lg" style="color:rgba(244,236,219,0.85); font-size:1.05rem">
          Every unit at Hiyswa Jambu is set up the same way — so there are no bad picks, only yours. A king bed and a queen bed, a fully-equipped kitchen, a living room for the late-night conversations, a clean bathroom, and four fresh towels waiting for you.
        </p>
        <p style="color:rgba(244,236,219,0.65); margin-top:1.25rem; font-size:0.95rem">
          Maximum occupancy is <strong style="color:var(--gold)">4 adults and 4 children</strong> per unit. Need more space? Book multiple units — they're a few steps apart, perfect for groups travelling together.
        </p>
      </div>
      <div class="room-features">
        <div class="room-feature">
          <i class="fas fa-bed"></i>
          <div>
            <div class="room-feature-title">1 King + 1 Queen</div>
            <div class="room-feature-sub">Beds per unit</div>
          </div>
        </div>
        <div class="room-feature">
          <i class="fas fa-couch"></i>
          <div>
            <div class="room-feature-title">Living Room</div>
            <div class="room-feature-sub">Private, air-conditioned</div>
          </div>
        </div>
        <div class="room-feature">
          <i class="fas fa-utensils"></i>
          <div>
            <div class="room-feature-title">Kitchen</div>
            <div class="room-feature-sub">Fully equipped</div>
          </div>
        </div>
        <div class="room-feature">
          <i class="fas fa-shower"></i>
          <div>
            <div class="room-feature-title">Bathroom</div>
            <div class="room-feature-sub">4 fresh towels provided</div>
          </div>
        </div>
      </div>
    </div>

    <div class="room-tiles">
      <!-- Weekday tile -->
      <div class="room-tile reveal">
        <div class="room-tile-img">
          <img src="https://picsum.photos/seed/cozy-bedroom-warm-light/800/520.jpg" alt="Weekday stay room">
          <div class="room-tile-tag">Sun – Thu</div>
        </div>
        <div class="room-tile-body">
          <div class="room-tile-title">Weekday Stay</div>
          <div class="room-tile-days">Sunday to Thursday · Per unit per night</div>
          <div class="room-tile-price">
            <span class="currency">RM</span>
            <span class="amount">270</span>
            <span class="unit">/ night</span>
          </div>
          <div class="room-tile-breakdown">
            <div>
              <span class="label">Unit price</span>
              <span class="val">RM 220.00</span>
            </div>
            <div>
              <span class="label">Booking deposit</span>
              <span class="val">RM 50.00</span>
            </div>
          </div>
          <p style="font-size:0.85rem; color:var(--muted); margin-bottom:1.25rem">
            Ideal for short midweek escapes, remote-work stays, or quiet family getaways without the weekend crowd.
          </p>
          <a href="#contact" class="room-tile-cta">
            <span>Book a weekday</span>
            <i class="fas fa-arrow-right"></i>
          </a>
        </div>
      </div>

      <!-- Weekend tile -->
      <div class="room-tile reveal reveal-delay-1">
        <div class="room-tile-img">
          <img src="https://picsum.photos/seed/luxury-bedroom-night-ambiance/800/520.jpg" alt="Weekend stay room">
          <div class="room-tile-tag gold">Fri – Sun &amp; Holidays</div>
        </div>
        <div class="room-tile-body">
          <div class="room-tile-title">Weekend &amp; Holiday</div>
          <div class="room-tile-days">Friday, Saturday, school &amp; public holidays</div>
          <div class="room-tile-price">
            <span class="currency">RM</span>
            <span class="amount">300</span>
            <span class="unit">/ night</span>
          </div>
          <div class="room-tile-breakdown">
            <div>
              <span class="label">Unit price</span>
              <span class="val">RM 250.00</span>
            </div>
            <div>
              <span class="label">Booking deposit</span>
              <span class="val">RM 50.00</span>
            </div>
          </div>
          <p style="font-size:0.85rem; color:var(--muted); margin-bottom:1.25rem">
            For long weekends, family gatherings, and celebrations. Book early — weekends at Hiyswa Jambu fill up quickly.
          </p>
          <a href="#contact" class="room-tile-cta">
            <span>Book a weekend</span>
            <i class="fas fa-arrow-right"></i>
          </a>
        </div>
      </div>
    </div>

    <div class="reveal reveal-delay-2 mt-12 p-6 rounded-2xl" style="background:rgba(244,236,219,0.05); border:1px solid rgba(244,236,219,0.1)">
      <div class="flex gap-4 items-start">
        <i class="fas fa-info-circle text-gold text-xl mt-1"></i>
        <div style="font-size:0.9rem; color:rgba(244,236,219,0.75)">
          <strong style="color:var(--cream)">Good to know:</strong> The RM 50 booking deposit is a security deposit, refundable after your stay subject to unit condition. Total nightly rate (RM 270 weekday / RM 300 weekend) is per unit, not per person — split among four adults, that's a steal. Maximum 4 adults and 4 children per unit.
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Facilities -->
<section id="facilities" class="section facilities-section">
  <div class="section-inner">
    <div class="reveal section-num">03 — Facilities</div>
    <h2 class="section-title reveal reveal-delay-1">Everything you need, <em>just steps</em><br>from your door.</h2>
    <p class="reveal reveal-delay-2 text-muted mt-6 max-w-2xl">Six on-site facilities, all included for guests. No tokens, no extra charges — just walk out and enjoy.</p>

    <div class="facilities-grid">
      <div class="facility-card reveal">
        <img src="https://picsum.photos/seed/resort-swimming-pool-blue/800/1000.jpg" alt="Swimming pool">
        <div class="facility-num">01</div>
        <div class="facility-content">
          <div class="facility-icon"><i class="fas fa-swimming-pool"></i></div>
          <div class="facility-name">Swimming Pool</div>
          <div class="facility-desc">A generous main pool for laps, lounging, or just cooling off under the Perlis sun.</div>
        </div>
      </div>
      <div class="facility-card reveal reveal-delay-1">
        <img src="https://picsum.photos/seed/waterpark-slides-children/800/1000.jpg" alt="Mini water park">
        <div class="facility-num">02</div>
        <div class="facility-content">
          <div class="facility-icon"><i class="fas fa-water"></i></div>
          <div class="facility-name">Mini Water Park</div>
          <div class="facility-desc">Splash pads and small slides — built for the little ones to spend the whole afternoon in.</div>
        </div>
      </div>
      <div class="facility-card reveal reveal-delay-2">
        <img src="https://picsum.photos/seed/futsal-court-night-lights/800/1000.jpg" alt="Futsal court">
        <div class="facility-num">03</div>
        <div class="facility-content">
          <div class="facility-icon"><i class="fas fa-futbol"></i></div>
          <div class="facility-name">Futsal Court</div>
          <div class="facility-desc">Floodlit, all-weather court. Bring your boots — or borrow a ball at the front desk.</div>
        </div>
      </div>
      <div class="facility-card reveal">
        <img src="https://picsum.photos/seed/parking-lot-event-space/800/1000.jpg" alt="Parking and event space">
        <div class="facility-num">04</div>
        <div class="facility-content">
          <div class="facility-icon"><i class="fas fa-square-parking"></i></div>
          <div class="facility-name">Spacious Parking &amp; Event Space</div>
          <div class="facility-desc">Ample parking for guests, plus an open area that doubles as a venue for private events.</div>
        </div>
      </div>
      <div class="facility-card reveal reveal-delay-1">
        <img src="https://picsum.photos/seed/bbq-grill-evening-fire/800/1000.jpg" alt="BBQ pit">
        <div class="facility-num">05</div>
        <div class="facility-content">
          <div class="facility-icon"><i class="fas fa-fire"></i></div>
          <div class="facility-name">Barbeque Place</div>
          <div class="facility-desc">Sheltered BBQ pits with seating — perfect for the long, slow dinners that holidays are made for.</div>
        </div>
      </div>
      <div class="facility-card reveal reveal-delay-2">
        <img src="https://picsum.photos/seed/bicycle-rental-vintage/800/1000.jpg" alt="Bicycle rental">
        <div class="facility-num">06</div>
        <div class="facility-content">
          <div class="facility-icon"><i class="fas fa-bicycle"></i></div>
          <div class="facility-name">Bicycle Renting</div>
          <div class="facility-desc">Cruiser bikes available by the hour or day. Arau's flat roads and paddy fields are best seen on two wheels.</div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Gallery -->
<section id="gallery" class="section gallery-section">
  <div class="section-inner">
    <div class="reveal section-num">04 — Gallery</div>
    <div class="gallery-intro">
      <h2 class="section-title reveal reveal-delay-1">Moments <em>from Hiyswa</em><br>Jambu.</h2>
      <p class="reveal reveal-delay-2 text-muted">A handful of frames from mornings by the pool, afternoons on the bikes, and evenings around the BBQ pit. Hover any photo to see the story behind it.</p>
    </div>

    <!-- Masonry-style grid -->
    <div class="gallery-grid">
      <div class="gallery-item h-big reveal">
        <img src="https://picsum.photos/seed/homestay-night-warm-lights/900/900.jpg" alt="Night view">
        <div class="gallery-caption">
          <div class="cat">Night view</div>
          <div class="title">The property after sunset</div>
        </div>
      </div>
      <div class="gallery-item reveal reveal-delay-1">
        <img src="https://picsum.photos/seed/pool-side-deck-chair/600/400.jpg" alt="Poolside">
        <div class="gallery-caption">
          <div class="cat">Facilities</div>
          <div class="title">Poolside afternoons</div>
        </div>
      </div>
      <div class="gallery-item h-tall reveal reveal-delay-2">
        <img src="https://picsum.photos/seed/living-room-cozy-malay/600/900.jpg" alt="Living room">
        <div class="gallery-caption">
          <div class="cat">Interior</div>
          <div class="title">Living room, unit 4</div>
        </div>
      </div>
      <div class="gallery-item reveal">
        <img src="https://picsum.photos/seed/bedroom-king-cozy/600/400.jpg" alt="Bedroom">
        <div class="gallery-caption">
          <div class="cat">Interior</div>
          <div class="title">King bedroom</div>
        </div>
      </div>
      <div class="gallery-item h-wide reveal reveal-delay-1">
        <img src="https://picsum.photos/seed/family-bbq-evening-malaysia/900/400.jpg" alt="Family BBQ">
        <div class="gallery-caption">
          <div class="cat">Guest experience</div>
          <div class="title">BBQ night with the family</div>
        </div>
      </div>
      <div class="gallery-item reveal reveal-delay-2">
        <img src="https://picsum.photos/seed/kitchen-modern-clean/600/400.jpg" alt="Kitchen">
        <div class="gallery-caption">
          <div class="cat">Interior</div>
          <div class="title">Kitchen, fully equipped</div>
        </div>
      </div>
      <div class="gallery-item reveal">
        <img src="https://picsum.photos/seed/children-waterpark-splash/600/400.jpg" alt="Kids at water park">
        <div class="gallery-caption">
          <div class="cat">Guest experience</div>
          <div class="title">Splash time at the mini water park</div>
        </div>
      </div>
      <div class="gallery-item h-wide reveal reveal-delay-1">
        <img src="https://picsum.photos/seed/paddy-field-perlis-sunset/900/400.jpg" alt="Paddy field">
        <div class="gallery-caption">
          <div class="cat">Around Arau</div>
          <div class="title">Paddy fields, five minutes away</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Night views carousel -->
  <div class="night-section">
    <div class="night-header">
      <div class="reveal section-num" style="color:var(--gold)">— Night Views</div>
      <h2 class="font-display text-cream mt-4" style="font-size:clamp(2rem,4.5vw,3.5rem); font-weight:300; line-height:1.05">
        When the lights <em style="color:var(--blush); font-style:italic">come on.</em>
      </h2>
      <p class="text-cream mt-4 max-w-xl" style="opacity:0.75">There's a particular kind of quiet at Hiyswa Jambu after dark. The pool glows, the BBQ pit crackles, and the units warm up like lanterns along the lane.</p>
    </div>
    <div class="night-track-wrap">
      <div class="night-track">
        <div class="night-card">
          <img src="https://picsum.photos/seed/night-pool-glow-blue/720/900.jpg" alt="Pool at night">
          <div class="night-card-content">
            <div class="time">07:45 PM</div>
            <div class="title">The pool, lit from below</div>
          </div>
        </div>
        <div class="night-card">
          <img src="https://picsum.photos/seed/night-bbq-smoke-warm/720/900.jpg" alt="BBQ at night">
          <div class="night-card-content">
            <div class="time">08:30 PM</div>
            <div class="title">BBQ pit in full swing</div>
          </div>
        </div>
        <div class="night-card">
          <img src="https://picsum.photos/seed/night-villa-windows-warm/720/900.jpg" alt="Units at night">
          <div class="night-card-content">
            <div class="time">09:15 PM</div>
            <div class="title">Units glowing down the lane</div>
          </div>
        </div>
        <div class="night-card">
          <img src="https://picsum.photos/seed/night-futsal-court-floodlight/720/900.jpg" alt="Futsal at night">
          <div class="night-card-content">
            <div class="time">10:00 PM</div>
            <div class="title">Futsal under the floodlights</div>
          </div>
        </div>
        <div class="night-card">
          <img src="https://picsum.photos/seed/night-garden-path-lights/720/900.jpg" alt="Garden path at night">
          <div class="night-card-content">
            <div class="time">10:45 PM</div>
            <div class="title">A walk back along the garden path</div>
          </div>
        </div>
        <!-- Duplicate for seamless loop -->
        <div class="night-card">
          <img src="https://picsum.photos/seed/night-pool-glow-blue/720/900.jpg" alt="Pool at night">
          <div class="night-card-content">
            <div class="time">07:45 PM</div>
            <div class="title">The pool, lit from below</div>
          </div>
        </div>
        <div class="night-card">
          <img src="https://picsum.photos/seed/night-bbq-smoke-warm/720/900.jpg" alt="BBQ at night">
          <div class="night-card-content">
            <div class="time">08:30 PM</div>
            <div class="title">BBQ pit in full swing</div>
          </div>
        </div>
        <div class="night-card">
          <img src="https://picsum.photos/seed/night-villa-windows-warm/720/900.jpg" alt="Units at night">
          <div class="night-card-content">
            <div class="time">09:15 PM</div>
            <div class="title">Units glowing down the lane</div>
          </div>
        </div>
        <div class="night-card">
          <img src="https://picsum.photos/seed/night-futsal-court-floodlight/720/900.jpg" alt="Futsal at night">
          <div class="night-card-content">
            <div class="time">10:00 PM</div>
            <div class="title">Futsal under the floodlights</div>
          </div>
        </div>
        <div class="night-card">
          <img src="https://picsum.photos/seed/night-garden-path-lights/720/900.jpg" alt="Garden path at night">
          <div class="night-card-content">
            <div class="time">10:45 PM</div>
            <div class="title">A walk back along the garden path</div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Guest memories -->
  <div class="memories-section">
    <div class="reveal section-num">— Guest Memories</div>
    <h2 class="font-display text-forest reveal reveal-delay-1" style="font-size:clamp(2rem,4.5vw,3.5rem); font-weight:300; line-height:1.05; letter-spacing:-0.02em">
      Postcards <em style="font-style:italic; color:var(--terra)">from our guests.</em>
    </h2>
    <p class="text-muted mt-4 reveal reveal-delay-2 max-w-xl">A few of the kind words and moments shared by guests who've stayed with us. Tag us on Instagram <strong style="color:var(--forest)">@hiyswajambu</strong> to be featured here.</p>

    <div class="memories-grid">
      <div class="polaroid reveal">
        <div class="polaroid-img">
          <img src="https://picsum.photos/seed/family-stay-malaysia-kids/600/600.jpg" alt="Family stay">
        </div>
        <div class="polaroid-caption">"The kids didn't want to leave the water park."</div>
        <div class="polaroid-meta">— Aisyah &amp; family, KL · April</div>
      </div>
      <div class="polaroid reveal reveal-delay-1">
        <div class="polaroid-img">
          <img src="https://picsum.photos/seed/friends-bbq-night-gathering/600/600.jpg" alt="Friends BBQ">
        </div>
        <div class="polaroid-caption">"Best BBQ pit in Perlis, hands down."</div>
        <div class="polaroid-meta">— Hafiz &amp; friends, Penang · June</div>
      </div>
      <div class="polaroid reveal reveal-delay-2">
        <div class="polaroid-img">
          <img src="https://picsum.photos/seed/couple-honeymoon-quiet/600/600.jpg" alt="Couple stay">
        </div>
        <div class="polaroid-caption">"Quiet, clean, and so thoughtfully set up."</div>
        <div class="polaroid-meta">— Wei &amp; Mei, Singapore · May</div>
      </div>
      <div class="polaroid reveal">
        <div class="polaroid-img">
          <img src="https://picsum.photos/seed/group-photo-futsal-team/600/600.jpg" alt="Futsal group">
        </div>
        <div class="polaroid-caption">"Futsal under the lights, then BBQ till midnight."</div>
        <div class="polaroid-meta">— Team Rajawali, Alor Setar · August</div>
      </div>
      <div class="polaroid reveal reveal-delay-1">
        <div class="polaroid-img">
          <img src="https://picsum.photos/seed/elderly-couple-garden-walk/600/600.jpg" alt="Quiet getaway">
        </div>
        <div class="polaroid-caption">"We woke up to birdsong. Actual birdsong."</div>
        <div class="polaroid-meta">— Pak Samat &amp; Mak Som, Kedah · July</div>
      </div>
      <div class="polaroid reveal reveal-delay-2">
        <div class="polaroid-img">
          <img src="https://picsum.photos/seed/bicycle-ride-paddy-evening/600/600.jpg" alt="Bicycle ride">
        </div>
        <div class="polaroid-caption">"Cycled to the paddy fields at sunset. Unreal."</div>
        <div class="polaroid-meta">— Daniel &amp; Sara, KL · September</div>
      </div>
    </div>
  </div>
</section>

<!-- Location -->
<section id="location" class="section location-section">
  <div class="section-inner">
    <div class="reveal section-num">05 — Location</div>
    <h2 class="section-title reveal reveal-delay-1">Find us in <em>Arau,</em><br>the royal town of Perlis.</h2>
    <p class="reveal reveal-delay-2 text-muted mt-6 max-w-2xl">Hiyswa Jambu Homestay is a five-minute drive from Arau town, fifteen minutes from Kangar, and roughly thirty minutes from the Thai border at Padang Besar. Easy to reach, hard to leave.</p>

    <div class="location-grid">
      <div class="map-wrap reveal">
        <iframe
          src="https://www.google.com/maps?q=Arau,Perlis,Malaysia&z=14&output=embed"
          loading="lazy" referrerpolicy="no-referrer-when-downgrade" title="Hiyswa Jambu Homestay location map"></iframe>
      </div>
      <div class="location-info reveal reveal-delay-1">
        <div class="location-card">
          <div class="icon"><i class="fas fa-map-marker-alt"></i></div>
          <div>
            <h4>Address</h4>
            <p>Hiyswa Jambu Homestay, Arau, 02600 Arau, Perlis, Malaysia</p>
          </div>
        </div>
        <div class="location-card">
          <div class="icon"><i class="fas fa-route"></i></div>
          <div>
            <h4>Getting here</h4>
            <p>From North-South Expressway, take the Arau exit. Follow Jalan Kangar–Arau for 5 minutes. We're on the left, just past the jambu orchard.</p>
          </div>
        </div>
        <div class="location-card">
          <div class="icon"><i class="fas fa-compass"></i></div>
          <div class="w-full">
            <h4>Nearby attractions</h4>
            <div class="nearby-list">
              <div class="nearby-item">
                <span class="nearly-item-name">Arau Railway Station</span>
                <span class="nearby-item-dist">3 KM</span>
              </div>
              <div class="nearby-item">
                <span class="nearly-item-name">Kangar Town Centre</span>
                <span class="nearby-item-dist">10 KM</span>
              </div>
              <div class="nearby-item">
                <span class="nearly-item-name">Universiti Malaysia Perlis (UniMAP)</span>
                <span class="nearby-item-dist">6 KM</span>
              </div>
              <div class="nearby-item">
                <span class="nearly-item-name">Padang Besar Border (Thailand)</span>
                <span class="nearby-item-dist">28 KM</span>
              </div>
              <div class="nearby-item">
                <span class="nearly-item-name">Kuala Perlis Ferry Terminal</span>
                <span class="nearby-item-dist">22 KM</span>
              </div>
              <div class="nearby-item">
                <span class="nearly-item-name">Gua Kelam Recreational Park</span>
                <span class="nearby-item-dist">33 KM</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Contact -->
<section id="contact" class="section contact-section">
  <div class="section-inner">
    <div class="reveal section-num">06 — Contact &amp; Booking</div>
    <h2 class="section-title reveal reveal-delay-1">Book your <em>stay at</em><br>Hiyswa Jambu.</h2>
    <p class="reveal reveal-delay-2 mt-6 max-w-2xl" style="color:rgba(244,236,219,0.75)">Send us your details and preferred dates — we'll get back to you within 24 hours to confirm availability and arrange your booking deposit.</p>

    <div class="contact-grid mt-12">
      <form class="contact-form reveal" onsubmit="return handleSubmit(event)">
        <div class="form-row">
          <div class="form-field">
            <label>Full name</label>
            <input type="text" name="name" required placeholder="e.g. Aisyah Rahmat">
          </div>
          <div class="form-field">
            <label>Phone number</label>
            <input type="tel" name="phone" required placeholder="e.g. +60 12-345 6789">
          </div>
        </div>
        <div class="form-row">
          <div class="form-field">
            <label>Check-in date</label>
            <input type="date" name="checkin" required>
          </div>
          <div class="form-field">
            <label>Check-out date</label>
            <input type="date" name="checkout" required>
          </div>
        </div>
        <div class="form-row">
          <div class="form-field">
            <label>Number of units</label>
            <select name="units">
              <option>1 unit</option>
              <option>2 units</option>
              <option>3 units</option>
              <option>4 units</option>
              <option>5+ units (please specify in notes)</option>
            </select>
          </div>
          <div class="form-field">
            <label>Guests</label>
            <select name="guests">
              <option>2 guests</option>
              <option>4 guests</option>
              <option>6 guests</option>
              <option>8 guests (max per unit)</option>
              <option>More than 8 (multiple units)</option>
            </select>
          </div>
        </div>
        <div class="form-field">
          <label>Notes / special requests</label>
          <textarea name="notes" rows="3" placeholder="Tell us if you're celebrating something, need BBQ pit access, bicycle rental, etc."></textarea>
        </div>
        <button type="submit" class="btn btn-primary mt-4" style="align-self:flex-start">
          Send booking request <i class="fas fa-paper-plane text-xs"></i>
        </button>
      </form>

      <div class="reveal reveal-delay-1">
        <h3 class="font-display text-3xl text-cream mb-6" style="font-weight:300">Or reach us <em style="color:var(--gold); font-style:italic">directly.</em></h3>
        <p style="color:rgba(244,236,219,0.7); margin-bottom:2rem">For instant responses, WhatsApp is the fastest. We're online from 9am to 10pm daily.</p>
        <div class="contact-cta">
          <a href="https://wa.me/60123456789" class="contact-channel">
            <i class="fab fa-whatsapp"></i>
            <div>
              <div class="label">WhatsApp</div>
              <div class="val">+60 12-345 6789</div>
            </div>
          </a>
          <a href="tel:+60123456789" class="contact-channel">
            <i class="fas fa-phone"></i>
            <div>
              <div class="label">Call</div>
              <div class="val">+60 12-345 6789</div>
            </div>
          </a>
        </div>
        <div class="contact-cta">
          <a href="mailto:hello@hiyswajambu.my" class="contact-channel">
            <i class="fas fa-envelope"></i>
            <div>
              <div class="label">Email</div>
              <div class="val">hello@hiyswajambu.my</div>
            </div>
          </a>
          <a href="https://instagram.com" class="contact-channel">
            <i class="fab fa-instagram"></i>
            <div>
              <div class="label">Instagram</div>
              <div class="val">@hiyswajambu</div>
            </div>
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Footer -->
<footer>
  <div class="footer-inner">
    <div class="footer-grid">
      <div>
        <div class="footer-brand">Hiyswa <span class="text-terra">Jambu</span></div>
        <p class="footer-tag">A serene homestay in Arau, Perlis. Eight units, sixteen bedrooms, and a quiet corner of northern Malaysia to call your own.</p>
        <div class="footer-social mt-6">
          <a href="#" aria-label="Instagram"><i class="fab fa-instagram"></i></a>
          <a href="#" aria-label="Facebook"><i class="fab fa-facebook-f"></i></a>
          <a href="#" aria-label="TikTok"><i class="fab fa-tiktok"></i></a>
          <a href="https://wa.me/60123456789" aria-label="WhatsApp"><i class="fab fa-whatsapp"></i></a>
        </div>
      </div>
      <div class="footer-col">
        <h5>Explore</h5>
        <a href="#about">About</a>
        <a href="#rooms">Rooms &amp; Rates</a>
        <a href="#facilities">Facilities</a>
        <a href="#gallery">Gallery</a>
        <a href="#location">Location</a>
      </div>
      <div class="footer-col">
        <h5>Contact</h5>
        <a href="tel:+60123456789">+60 12-345 6789</a>
        <a href="mailto:hello@hiyswajambu.my">hello@hiyswajambu.my</a>
        <a href="https://wa.me/60123456789">WhatsApp us</a>
      </div>
      <div class="footer-col">
        <h5>Visit</h5>
        <a href="#location">Arau, Perlis</a>
        <a href="#location">02600, Malaysia</a>
        <a href="#location">Get directions</a>
      </div>
    </div>
    <div class="footer-bottom">
      <div>© 2024 Hiyswa Jambu Homestay. All rights reserved.</div>
      <div>Designed with care in Arau, Perlis.</div>
    </div>
  </div>
</footer>

<!-- Back to top -->
<button class="back-top" id="backTop" aria-label="Back to top">
  <i class="fas fa-arrow-up"></i>
</button>

</div>

<!-- Toast -->
<div id="toast" style="position:fixed; bottom:2rem; left:50%; transform:translateX(-50%) translateY(20px); background:var(--forest); color:var(--cream); padding:1rem 1.5rem; border-radius:999px; z-index:200; opacity:0; transition:all 0.4s ease; box-shadow:0 15px 40px rgba(31,53,44,0.3); font-size:0.9rem; pointer-events:none;">
  <i class="fas fa-check-circle" style="color:var(--gold); margin-right:0.5rem"></i>
  <span id="toastMsg">Booking request sent. We'll be in touch within 24 hours.</span>
</div>

<script>
  // Loader
  window.addEventListener('load', () => {
    setTimeout(() => {
      document.getElementById('loader').classList.add('hidden');
    }, 1600);
  });

  // Custom cursor
  const dot = document.getElementById('cursorDot');
  const ring = document.getElementById('cursorRing');
  let mouseX = 0, mouseY = 0, ringX = 0, ringY = 0;
  document.addEventListener('mousemove', e => {
    mouseX = e.clientX; mouseY = e.clientY;
    dot.style.left = mouseX + 'px';
    dot.style.top = mouseY + 'px';
  });
  function animateRing() {
    ringX += (mouseX - ringX) * 0.18;
    ringY += (mouseY - ringY) * 0.18;
    ring.style.left = ringX + 'px';
    ring.style.top = ringY + 'px';
    requestAnimationFrame(animateRing);
  }
  animateRing();
  document.querySelectorAll('a, button, .facility-card, .gallery-item, .room-tile, .polaroid').forEach(el => {
    el.addEventListener('mouseenter', () => ring.classList.add('hover'));
    el.addEventListener('mouseleave', () => ring.classList.remove('hover'));
  });

  // Nav scroll effect
  const nav = document.getElementById('nav');
  const backTop = document.getElementById('backTop');
  window.addEventListener('scroll', () => {
    if (window.scrollY > 60) nav.classList.add('scrolled');
    else nav.classList.remove('scrolled');
    if (window.scrollY > 600) backTop.classList.add('show');
    else backTop.classList.remove('show');
  });
  backTop.addEventListener('click', () => window.scrollTo({top:0, behavior:'smooth'}));

  // Mobile menu
  const toggle = document.getElementById('mobileToggle');
  const navLinks = document.getElementById('navLinks');
  toggle.addEventListener('click', () => {
    toggle.classList.toggle('open');
    navLinks.classList.toggle('open');
  });
  navLinks.querySelectorAll('a').forEach(a => {
    a.addEventListener('click', () => {
      toggle.classList.remove('open');
      navLinks.classList.remove('open');
    });
  });

  // Active nav link on scroll
  const sections = document.querySelectorAll('section[id]');
  const navItems = document.querySelectorAll('.nav-link');
  window.addEventListener('scroll', () => {
    let current = '';
    sections.forEach(s => {
      const top = s.offsetTop - 120;
      if (window.scrollY >= top) current = s.id;
    });
    navItems.forEach(a => {
      a.classList.remove('active');
      if (a.getAttribute('href') === '#' + current) a.classList.add('active');
    });
  });

  // Reveal on scroll
  const observer = new IntersectionObserver(entries => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        e.target.classList.add('in');
        observer.unobserve(e.target);
      }
    });
  }, { threshold: 0.15, rootMargin: '0px 0px -50px 0px' });
  document.querySelectorAll('.reveal, .img-reveal').forEach(el => observer.observe(el));

  // Stat counter
  const counters = document.querySelectorAll('.stat-num em');
  const counterObs = new IntersectionObserver(entries => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        const el = e.target;
        const text = el.textContent;
        const num = parseInt(text);
        if (!isNaN(num) && num <= 100) {
          let current = 0;
          const step = Math.max(1, Math.ceil(num / 30));
          const tick = () => {
            current += step;
            if (current >= num) { el.textContent = num; }
            else { el.textContent = current; requestAnimationFrame(tick); }
          };
          el.textContent = '0';
          setTimeout(tick, 200);
        }
        counterObs.unobserve(el);
      }
    });
  }, { threshold: 0.5 });
  counters.forEach(c => counterObs.observe(c));

  // Hero parallax
  const heroBg = document.querySelector('.hero-bg img');
  window.addEventListener('scroll', () => {
    if (window.scrollY < window.innerHeight) {
      heroBg.style.transform = `translateY(${window.scrollY * 0.3}px) scale(${1 + window.scrollY * 0.0005})`;
    }
  });

  // Form submission
  function handleSubmit(e) {
    e.preventDefault();
    const toast = document.getElementById('toast');
    toast.style.opacity = '1';
    toast.style.transform = 'translateX(-50%) translateY(0)';
    setTimeout(() => {
      toast.style.opacity = '0';
      toast.style.transform = 'translateX(-50%) translateY(20px)';
    }, 4000);
    e.target.reset();
    return false;
  }

  // Set min date on date inputs to today
  const today = new Date().toISOString().split('T')[0];
  document.querySelectorAll('input[type="date"]').forEach(i => i.min = today);
</script>

</body>
</html>